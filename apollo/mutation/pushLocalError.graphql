mutation pushLocalError($message: String!, $name: String!, $statusCode: Number!) {
  pushLocalError(message: $message, name: $name, statusCode: $statusCode) @client {
    message
    name
    statusCode
  }
}
